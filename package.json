{
  "name": "software-testing-assignment",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx jest test",
    "coverage": "npx jest --coverage",
    "coverageReport": "npx jest --coverage && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "jest": "^29.3.1"
  },
  "devDependencies": {
    "coveralls": "^3.1.1"
  }
}
