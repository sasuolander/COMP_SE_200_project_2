describe("TC1: In SPPAC page when searching item, it returns what is searched.", () => {
  test("dummy", () => {
    expect(mathOperations.sum(1, 2)).toBe(3);
  });
});


describe("TC2: In SPPAC page when searching item by using category, it returns what is searched.", () => {
    test("dummy", () => {
      expect(mathOperations.sum(1, 2)).toBe(3);
    });
  });


  describe("TC3:Customer can add product to cart in SPPAC pages.", () => {
    test("dummy", () => {
      expect(mathOperations.sum(1, 2)).toBe(3);
    });
  });


  describe("TC4: Customer can remove product from cart in SCP pages.", () => {
    test("dummy", () => {
      expect(mathOperations.sum(1, 2)).toBe(3);
    });
  });

  describe("TC5: SCP page calculate total cost of all products.", () => {
    test("dummy", () => {
      expect(mathOperations.sum(1, 2)).toBe(3);
    });
  });
